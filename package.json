{
  "name": "keka-scheduler",
  "version": "1.0.0",
  "description": "Auto punch-in/punch-out scheduler for Keka attendance",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "setup-session": "node scripts/setup-session.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "pino": "^9.5.0",
    "playwright": "^1.48.0"
  }
}
